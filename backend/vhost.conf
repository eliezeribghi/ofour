<VirtualHost *:80>
    DocumentRoot ./backend/app/public

    <Directory "/etc/apache2/backend">
        AllowOverride all
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
